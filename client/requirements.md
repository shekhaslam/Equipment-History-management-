## Packages
jspdf | Generate PDF reports client-side
jspdf-autotable | Create tables in PDF reports
lucide-react | Beautiful icons for the UI
date-fns | Formatting dates for display and inputs

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
